import 'package:get/get.dart';

class AboutAppController extends GetxController {
  // Untuk saat ini, controller ini masih kosong
  // Nanti bisa ditambahkan logika seperti mengambil versi aplikasi, dll.
  final appVersion = "1.0.0".obs;
}
